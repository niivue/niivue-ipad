{
  "name": "niivue-ipad",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd src/niivue && npm install && npm run build && cd ../.. && vite",
    "build": "cd src/niivue && npm install && npm run build && cd ../.. && vite build",
    "preview": "vite preview",
    "postinstall": "cd src/niivue && npm install && npm run build"
  },
  "dependencies": {
    "@capacitor/core": "^4.6.1",
    "@capacitor/ios": "^4.6.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^4.6.1",
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "@vitejs/plugin-react": "^3.0.0",
    "vite": "^4.0.0"
  }
}
